const calculatorButton = document.querySelector('.calculatorButton');

calculatorButton.addEventListener('click', selectButton(e));

function selectButton() {
    console.log(calculatorButton.innerHTML);
};